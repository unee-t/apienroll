build:
    main: main.go
    binary: apienroll
    goos:
        - windows
        - darwin
        - linux
    goarch:
        - amd64

dockers:
    - image: uneet/apienroll
      tag_templates:
          - '{{ .Tag }}'
          - 'v{{ .Major }}.{{ .Minor }}'
          - 'latest'
